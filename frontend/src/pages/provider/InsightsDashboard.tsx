/**
 * Legacy route wrapper — /provider/insights redirects to the unified analytics hub.
 * Panel components live in components/analytics/provider/insights-tabs/.
 */
export { default } from './ProviderInsightsRedirect';
